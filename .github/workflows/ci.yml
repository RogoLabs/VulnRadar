name: CI

on:
  push:
    branches: [main]
    paths:
      - "**.py"
      - "pyproject.toml"
      - "requirements*.txt"
      - "watchlist.yaml"
      - "watchlist.example.yaml"
      - ".github/workflows/ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "**.py"
      - "pyproject.toml"
      - "requirements*.txt"
      - "watchlist.yaml"
      - "watchlist.example.yaml"
      - ".github/workflows/ci.yml"

permissions:
  contents: read

jobs:
  test:
    # Only run CI on the main repo, not on forks (saves their Actions minutes)
    if: github.repository == 'RogoLabs/VulnRadar'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Validate watchlist.yaml
        run: |
          pip install pyyaml -q
          python scripts/validate_watchlist.py

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Lint with ruff
        run: |
          ruff check --output-format=github .

      - name: Type check with mypy
        run: |
          mypy etl.py notify.py --ignore-missing-imports || echo "::warning::mypy found type issues (non-blocking)"

      - name: Run tests
        run: |
          pytest -v --tb=short

  lint:
    # Quick lint job that runs on all PRs (even from forks) but very fast
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"

      - name: Install ruff
        run: pip install ruff

      - name: Lint
        run: ruff check --output-format=github .
